
  # Extend AuditPatchX with Table Sync

  This is a code bundle for Extend AuditPatchX with Table Sync. The original project is available at https://www.figma.com/design/xbu9VEuHDmKEK0v0SQQHcj/Extend-AuditPatchX-with-Table-Sync.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  